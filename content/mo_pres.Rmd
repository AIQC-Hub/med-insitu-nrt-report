---
title: "Pressure (AR)"
description: |
  Basic analysis of the pressure data from the Mediterranean Sea in situ NRT observations.
output:
  distill::distill_article:
    toc: true
    toc_depth: 2
---

```{r, init, include = FALSE}
knitr::opts_chunk$set(echo=FALSE)

# Common functions
r_func_path <- "./_func"
r_funcs <- c("libraries.Rmd", "common.Rmd", "common_mo.Rmd", "var.Rmd")

# Variables
var <- "pres"
var_name <- "Pressure"
var_label <- "Pres"

```

```{r, import, include = FALSE, child=file.path(r_func_path, r_funcs)}
```

```{r, location_filtering, include = FALSE}
src <- knitr::knit_expand(t_location_filtering, df=df_name, df_filtered=df_filtered_name)
res <- knitr::knit_child(text = src, quiet = TRUE)
```

## 1. Basic Information
```{r, basic_info, results = 'asis'}
res <- knitr::knit_expand(t_v_basic_info)
cat(res, sep = '\n')
```

---

## 2. Summary Statistics
```{r, summary_stats, results = 'asis'}
src <- knitr::knit_expand(t_v_summary_stats, df=df_filtered_name)
res <- knitr::knit_child(text = src, quiet = TRUE)
cat(res, sep = '\n')
```

---

## 3. Distributions Over Time and Location
```{r, distributions, results = 'asis'}
src <- knitr::knit_expand(t_v_distributions)
res <- knitr::knit_child(text = src, quiet = TRUE)
cat(res, sep = '\n')
```

---

```{r, cleanup, include = FALSE}
rm(list = ls())
```
